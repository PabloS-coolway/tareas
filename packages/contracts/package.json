{
  "name": "@yorga/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos y contratos compartidos entre la API y el front (etiquetas Coolway).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(contracts: sin tests)\" && exit 0",
    "lint": "echo ok"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
